name: BotHire-Shield Protocol Guard & Enforcer

on:
  push:
    branches: [ main ]

jobs:
  validate-and-enforce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Foundry
        uses: foundry-rs/foundry-toolchain@v1

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install AJV
        run: npm install -g ajv-cli

      - name: Run x402 Protocol Check
        id: verify_step
        continue-on-error: true
        run: |
          ajv validate -s ./schemas/aSLA_schema.json -d ./schemas/sample_aSLA.json

      - name: 🚨 On-Chain Enforcement (Base Sepolia)
        if: steps.verify_step.outcome == 'failure'
        env:
          RPC_URL: https://sepolia.base.org
          PRIVATE_KEY: ${{ secrets.STRATEGY_PRIVATE_KEY }}
          CONTRACT_ADDRESS: 0xa7D1299B45294e4F34fD0cF0da4100d78Df26090
        run: |
          echo "❌ 協議失敗！正在執行鏈上制裁..."
          BOT_ID=$(node -e "console.log(require('./schemas/sample_aSLA.json').bot_id || 'Unknown')")
          # 使用 cast 調用合約的 banBot 函數
          cast send $CONTRACT_ADDRESS "banBot(string,string)" "$BOT_ID" "Protocol Violation"             --rpc-url $RPC_URL             --private-key $PRIVATE_KEY
